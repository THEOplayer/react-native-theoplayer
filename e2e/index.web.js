// The plain web entry point. The e2e app is only meaningful when run through
// `cavynext run-web`, which swaps `index.test.web.js` into this place.
console.error('This app can only be used with react-native-cavynext-cli: run `npm run test:e2e:web`.');
